# Core platforms
steam
discord
protonup-qt

# Firmware
linux-firmware

# Graphics stack 
mesa
vulkan-tools

# Vulkan extras
vulkan-validation-layers

# Input / compatibility
SDL2

# Performance tools
gamemode
mangohud
goverlay

# Optional
# gamescope

# Controllers
steam-devices

# Audio (PipeWire)
pipewire
wireplumber
alsa-utils
easyeffects

# Optional Windows compatibility helpers
# wine
# winetricks

# System packages:
# sudo eopkg install $(cat gaming-packages-solus.txt)


# =========================
# Flatpak apps
# =========================

# Make sure Flatpak + Flathub are set up first
# sudo eopkg install flatpak
# flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo

# Install Flatpak apps
# flatpak install flathub \
# net.davidotek.pupgui2 \
# com.eduke32.EDuke32

# Flatpaks:
# flatpak install flathub $(cat flatpak-packages.txt)


# =========================
# EDuke32 note (Flatpak)
# =========================

# Place DUKE3D.GRP in:
# ~/.var/app/com.eduke32.EDuke32/.config/eduke32/


# verification checks - run commands separately one at a time
# 1. Check Vulkan driver (RADV)
      vulkaninfo | grep RADV
            # Expected: driverName = AMD RADV
# 2. Check GameMode daemon
      gamemoded -t
            # Expected: gamemoded is running
# 3. Check MangoHud injection
      # mangohud glxgears
      mangohud vkcube
            # Expected: spinning gears + MangoHud overlay
# 4. Confirm GPU + driver
      vulkaninfo | grep -E "driverName|deviceName"
            # Expected: AMD GPU + RADV